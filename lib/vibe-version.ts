/**
 * Shown in the main header as `vibe sync · v{...}`.
 * Bump the **patch** (third number) for any user-visible or data change; bump **minor** for
 * features; **major** for breaking behavior. Keep in sync with `package.json` `version` when you
 * release.
 */
export const VIBE_SYNC_VERSION = "1.0.1";
